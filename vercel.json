{
  "version": 2,
  "buildCommand": "npm install && cd apps/shell && npm install && npm run build",
  "outputDirectory": "apps/shell/dist",
  "routes": [
    {
      "src": "/assets/(.*)",
      "dest": "/assets/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
